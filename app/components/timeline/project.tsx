import {gitStores} from "~/stores/data";import {ProjectData} from "~/@types/user";import {convertLanguage, formatDate} from "~/lib/utils";import {Fade} from "react-awesome-reveal";import {Branch_Icon, UpdateIcon} from "~/assets/skill_icons/icons";const Project = () => {    const {project} = gitStores()    const handleNavigate = (url:string)=> {        window.open(url,"_blank")    }    return <div className="w-full grid grid-cols-1 lg:grid-cols-2 gap-4">        {project && project.slice(0, 8).map((item: ProjectData) =>            <Fade direction={"up"} key={`project-${item.id}`}>                {/* eslint-disable-next-line jsx-a11y/click-events-have-key-events,jsx-a11y/no-static-element-interactions */}                <div onClick={() => handleNavigate(item.html_url)}                     className={`cursor-pointer grid grid-cols-2 gap-6 p-4 animate-shimmer rounded-md border-solid border-[1px] border-slate-800 bg-[linear-gradient(110deg,#000103,45%,#1e2631,55%,#000103)] bg-[length:200%_100%] px-6 font-medium text-slate-400 transition-colors focus:outline-none focus:ring-2 focus:ring-slate-400 focus:ring-offset-2 focus:ring-offset-slate-50`}>                    <span>{item.name}</span>                    <span className={`truncate`}>{item.full_name}</span>                    <span className={`grid grid-cols-5 items-center justify-center`}><Branch_Icon                        className={`col-span-1 w-5 h-5`}/> {item.default_branch}</span>                    <span className={`grid grid-cols-5 items-center justify-center`}>                    {convertLanguage(item.language)}                </span>                    <span className={`grid grid-cols-5 items-center justify-center`}>                    <UpdateIcon className={`w-5 h-5`}/> {formatDate(item.updated_at)}                </span>                </div>            </Fade>        )}    </div>}export default Project