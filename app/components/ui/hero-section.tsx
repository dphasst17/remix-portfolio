import React from "react";import {BackgroundBeamsWithCollision} from "~/components/ui/background-beam";import { Fade } from "react-awesome-reveal";import {HeroSectionProps} from "~/@types/hero";import {Facebook_Icon, Github_Icon, LinkedIn_Icon} from "~/assets/skill_icons/icons";import {user_data} from "~/data/user";import Widget from "~/components/ui/widget";const HeroSection = () => {    const text:HeroSectionProps[] = [        {            character:'D',            class:'from-red-700 to-orange-700',            direction: "left"        },        {            character:'F',            class:'from-amber-700 to-yellow-700',            direction: "up"        },        {            character:'A',            class:'from-lime-700 to-green-700',            direction: "down"        },        {            character:'S',            class:'from-emerald-700 to-teal-700',            direction: "up"        },        {            character:'T',            class:'from-cyan-700 to-sky-700',            direction: "right"        }    ]    const contact: {icon: React.ReactNode, link: string}[] = [        {            icon:<Facebook_Icon className="w-10 min-w-10 h-10"/>,            link:user_data.facebook        },        {            icon:<Github_Icon className="w-10 min-w-10 h-10"/>,            link:user_data.github        },        {            icon:<LinkedIn_Icon className="w-10 min-w-10 h-10"/>,            link:user_data.linkedin        }    ]    return <div className="w-full h-screen">    <BackgroundBeamsWithCollision>        <Widget/>        <div className="tourney text-[250px] text-amber-100 grid grid-cols-5">            {text.map((t:HeroSectionProps,i:number) =>                <Fade key={t.character} direction={t.direction} delay={i*150}>                    <span key={t.character}                         className={`relative bg-clip-text bg-gradient-to-r text-transparent ${t.class} z-50`}>                        {t.character}                    </span>                </Fade>            )}        </div>        <div className="w-4/5 sm:w-3/5 md:w-2/5 lg:w-1/5 h-[40px] grid grid-cols-3 items-center ">            {contact.map((c:{icon: React.ReactNode, link: string},i:number) => (                <Fade key={c.link} direction={"up"} delay={i*150}>                    <a                        className="animate-shimmer rounded-md border-solid border-[1px] border-slate-800 bg-[linear-gradient(110deg,#000103,45%,#1e2631,55%,#000103)] bg-[length:200%_100%] px-6 font-medium text-slate-400 transition-colors focus:outline-none focus:ring-2 focus:ring-slate-400 focus:ring-offset-2 focus:ring-offset-slate-50 p-1 mx-3 h-full flex items-center justify-center"                        href={c.link} target="_blank" rel="noreferrer">                        {c.icon}                    </a>                </Fade>            ))}        </div>    </BackgroundBeamsWithCollision>    </div>}export default HeroSection